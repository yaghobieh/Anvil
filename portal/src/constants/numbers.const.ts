/** Layout and spacing (px). */
export const TOPBAR_HEIGHT_PX = 64;

export const CONTENT_MAX_WIDTH_PX = 896;

export const HERO_MIN_HEIGHT_VH = 72;

/** Search modal: max results when query is empty. */
export const SEARCH_DEFAULT_LIMIT = 10;

/** Sandbox debounce demo default delay (ms). */
export const SANDBOX_DEBOUNCE_MS_DEFAULT = 400;
